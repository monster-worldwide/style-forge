/**
 * Prefix used in Figma to indicate design system configuration elements.
 */
export const CONFIG_PREFIX = '.ds-config-';

/**
 * Prefix used in Figma to indicate ignored elements.
 */
export const IGNORE_PREFIX = '.ds-ignore-';

/**
 * Prefix used in Figma to indicate pinned elements.
 */
export const PIN_PREFIX = '.ds-pin-';

/**
 * Prefix used in Figma to indicate design system configuration elements.
 */
export const ICON_PREFIX = 'md-icon/';
